
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: result
  annotations:
    dev.okteto.com/generate-host: result
spec:
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: result
      port: 80