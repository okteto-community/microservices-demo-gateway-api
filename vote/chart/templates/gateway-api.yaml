
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vote
  annotations:
    dev.okteto.com/generate-host: vote
spec:
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: vote
      port: 8080